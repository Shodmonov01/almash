# Backend API origin.
# Local `npm run dev` proxies /api and /uploads here (avoids CORS).
# Production builds call this URL directly. Leave empty if UI is served from the same origin as the API.
VITE_API_URL=http://localhost:3001
